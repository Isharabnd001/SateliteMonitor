apiVersion: v1
kind: Service
metadata:
  name: satelite-control-deployment-2
spec:
  selector:
    app: satelite-control-deployment-2
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000